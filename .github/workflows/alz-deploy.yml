name: ALZ Deploy

on:
  push:
    branches: [main]
    paths:
      - 'environments/**'
      - 'modules/**'
      - '.github/workflows/**'
  pull_request:
    branches: [main]
    paths:
      - 'environments/**'
      - 'modules/**'
      - '.github/workflows/**'

permissions:
  id-token: write
  contents: read
  pull-requests: write
  actions: read

jobs:
  shared-03-management:
    uses: ./.github/workflows/layer-03-management.yml
    with:
      action: ${{ github.event_name == 'pull_request' && 'plan' || 'apply' }}
    secrets: inherit

  shared-04-hub:
    needs: shared-03-management
    uses: ./.github/workflows/layer-04-hub.yml
    with:
      action: ${{ github.event_name == 'pull_request' && 'plan' || 'apply' }}
    secrets: inherit

  dev-01-mg:
    needs: shared-04-hub
    uses: ./.github/workflows/layer-01-mg.yml
    with:
      action: ${{ github.event_name == 'pull_request' && 'plan' || 'apply' }}
      env: dev
    secrets: inherit

  dev-02-policy:
    needs: dev-01-mg
    uses: ./.github/workflows/layer-02-policy.yml
    with:
      action: ${{ github.event_name == 'pull_request' && 'plan' || 'apply' }}
      env: dev
    secrets: inherit

  dev-05-workload:
    needs: dev-02-policy
    uses: ./.github/workflows/layer-05-workload.yml
    with:
      action: ${{ github.event_name == 'pull_request' && 'plan' || 'apply' }}
      env: dev
    secrets: inherit

  shared-05-avnm:
    needs: dev-05-workload
    uses: ./.github/workflows/layer-05-avnm.yml
    with:
      action: ${{ github.event_name == 'pull_request' && 'plan' || 'apply' }}
    secrets: inherit

  # ── Prod jobs commented out until prod workload is deployed ──
  # prod-01-mg:
  # prod-02-policy:
  # prod-05-workload:
  # shared-05-avnm-final: